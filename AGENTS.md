# 代理与协作约定（AGENTS）

本文件适用于整个仓库（根目录作用域）。所有参与者（含自动化代理/AI 助手）在进行任何修改或沟通前，必须阅读并遵守以下约定。

## 编码规范（Python）
- 必须严格遵循 `docs/Python编码规范与风格指南.md` 中的规范执行（命名、导入顺序、行宽、注释与文档字符串、异常与日志、类型注解、测试与结构等）。
- 提交前请自行检查是否符合该规范；如与历史代码风格冲突，应以本规范为准并进行最小必要改动以保持一致性。
- 若引入新依赖或工具（如格式化/静态检查），需在提交说明中明确理由与配置（尽量最小化变更范围）。

## 沟通与文档
- 所有沟通、提交说明、评审意见与自动化回复一律使用中文。
- 面向用户/同事的输出应清晰、可执行、避免空话与重复；必要时给出简明的下一步指引。

## 禁止 Mock / Stub
- 严禁在代码、测试或文档中使用 mock、stub、占位实现来“冒充”真实功能。
- 若外部系统尚不可用或暂无法集成，应提供最小可运行的真实实现或明确标注 TODO/待集成点，并确保不会误导读者或影响生产可用性。

## 变更边界与一致性
- 只在任务所需的范围内进行最小且聚焦的变更；避免无关重构。
- 尊重现有结构与约定；新增文件与命名需与仓库整体风格一致。

## 提交流程建议
- 在本地通过格式/静态检查（若已配置），并确保示例与文档与实现一致。
- 对会影响使用方式的变更，更新对应文档/README，并给出迁移或使用说明。

（若与上层系统/运行环境的直接指令冲突，以上层直接指令为准；否则以本 AGENTS.md 约定优先。）

