# 推理 API 示例

包含 Python 与 JavaScript 简易示例，调用 `Story 5.3` 提供的 `/api/v1/inference` 接口。

1. 先在平台创建工作空间、部署模型，并通过 `/api/v1/inference/api-keys` 生成密钥。  
2. Python：参考 `sdk/python/llmft_client`。  
3. JavaScript：参考 `sdk/js/llmft-client`。

> 示例主要用于验证接口契约，真实生产环境请结合企业网络、鉴权与监控策略部署。
