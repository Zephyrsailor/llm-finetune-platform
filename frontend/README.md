# llm-finetune-platform Frontend Skeleton

本目录提供 React + Vite + Tailwind 的最小工程骨架，用于 Story 1.1 中构建认证界面。

## 本地开发

```bash
cd frontend
pnpm install
pnpm dev
```

Tailwind 已配置在 `tailwind.config.ts`，页面样式入口为 `src/styles.css`。登陆页面样例位于 `src/app/(auth)/LoginPage.tsx`，后续可在此基础上扩展注册、忘记密码等流程。
